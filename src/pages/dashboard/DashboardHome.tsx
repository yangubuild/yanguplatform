import { NavDashPromoCards } from "@/components/mass/navigation/NavDashPromoCards";

/**
 * Dashboard Home — the default landing view inside the App Shell.
 * Re-uses the existing promo cards content.
 */
export default function DashboardHome() {
  return <NavDashPromoCards />;
}
